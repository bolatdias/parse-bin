version: "3.9"


services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: "parsebin"
      POSTGRES_USER: "root"
      POSTGRES_PASSWORD: "password"
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"